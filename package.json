{
  "name": "Newone",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ sayedex/Newone",
    "create-local": "graph create --node http://localhost:8020/ sayedex/Newone",
    "remove-local": "graph remove --node http://localhost:8020/ sayedex/Newone",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sayedex/Newone"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
